import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.apexfit.gym',
  appName: 'ApexFit',
  webDir: 'dist/gym-management-system'
};

export default config;
